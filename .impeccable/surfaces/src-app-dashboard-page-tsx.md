---
version: 1
slug: "src-app-dashboard-page-tsx"
primary_target: "src/app/dashboard/page.tsx"
related_targets: ["src/components/dashboard/dashboard-shell.tsx"]
---

Scope: the authenticated overview and its shared application shell. Mode: Operate.

Audience and job: administrators, managers, and agents must understand performance inside their server-enforced scope, then move to real performance, agent, team, import, or administration routes.

Proof and content: active-version totals, activity states, hourly data, agent rows, import freshness, and reconciliation from the existing scoped data boundary. No invented comparisons or unsupported filters.

Direction: “The Operations Briefing” expressed through the “Decision Ladder” composition. An eight-column performance story leads; a four-column diagnostic rail keeps activity and data trust in view. The memorable moment is the immediate visual connection between live scope, core performance, detailed rows, and the active data version.

Constraints: preserve server components, role visibility, authorization, import versioning, and real action semantics; compact enterprise density; responsive navigation drawer; WCAG 2.2 AA; restrained state motion only.

Unresolved: chart ordering and table row count may be adjusted after desktop and mobile visual QA using real local data.
